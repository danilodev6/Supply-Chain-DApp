const Footer = () => {
  return (
    <footer className="fixed bottom-0 w-full p-6 text-center text-xs md:text-sm text-white-base">
      <p>© {new Date().getFullYear()} Supply Chain Web3 dApp.</p>
    </footer>
  );
};

export default Footer;
